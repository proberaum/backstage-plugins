import { analyticsModuleBrowserLogPlugin } from './plugin';

describe('analytics-module-browser-log', () => {
  it('should export plugin', () => {
    expect(analyticsModuleBrowserLogPlugin).toBeDefined();
  });
});
